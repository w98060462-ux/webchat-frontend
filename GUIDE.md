# WebChat 使用指南

> 写给每一位使用者。不讲技术，只讲你最关心的事：这个工具优势在哪、你的消息到底安不安全、以及怎么用好它。

---

## 这个工具是做什么的

WebChat 是一个聊天工具，支持：

- 和单个人私聊
- 建群、拉人进来一起聊
- 发送文件（图片、文档、视频……最大 1GB）

它和VX、QQ 最大的不同只有一点：**你们之间聊的内容，除了你们两个人，任何人都看不到——包括搭建这个服务器的人。**

---

## 你的消息到底安不安全

### 先说一个真实的问题

你在VX发一条消息，这条消息会先传到VX的服务器，VX把它存下来，再转发给对方。这意味着：

- VX的服务器上存着你所有的聊天记录
- 服务器被黑了，你的记录可能被人看到
- 运营商或有关部门可以要求调取记录
- 内部员工理论上有机会访问这些数据

这种现象只是"消息存在别人服务器上"这件事本身带来的风险。

### WebChat 是怎么做的

WebChat 里，**消息在离开你的手机/电脑之前就已经被加密了**，到了服务器的时候已经是一堆别人看不懂的乱码，服务器只负责把这堆乱码转发给对方，转发完就丢掉，**服务器上根本没有你的聊天内容**。

对方收到这堆乱码之后，用只有他自己才有的"钥匙"把它还原成你发的原文。

整个过程服务器始终看到的只是乱码，就算有人把服务器的数据全部拿走，也读不出你们聊了什么。

### 用一个生活中的比喻

想象你要给朋友寄一封信：

- **普通聊天工具**：信写完放进信封，邮局帮你寄，但邮局可以拆开看，看完再封好寄出去，还留了一份复印件在档案室。
- **WebChat**：信写完之后，你用一把只有你和朋友之间才有的特殊锁锁上，邮局拿到的是一个他们打不开的箱子，他们只负责把箱子送到朋友门口，朋友用自己的钥匙打开，邮局全程不知道里面是什么。

### 这把"锁"是怎么来的

你和每个聊天对象之间，都有一把独一无二的锁，这把锁是你们第一次建立连接时，由你们各自的设备自动协商生成的，**整个过程不需要经过服务器**，服务器也不知道这把锁是什么。

所以就算有人盯着你们之间所有的网络流量，他也只能看到乱码，因为这把锁从来没有在网络上传输过。

### 群聊也是加密的吗

是的。群聊有一把"群密钥"，这把钥匙在群成员之间共享，群消息用这把钥匙加密后再发出去。服务器收到的同样是乱码，只有群里的人才能解开。

当有人退出群组时，群密钥会自动更换，退出的人从此看不到新消息。

### 聊天记录存在哪里

**存在你自己的设备上**，具体来说是浏览器的本地存储空间（IndexedDB）。

- 服务器上没有你的聊天记录
- 换了设备，历史记录不会自动同步过来（这是故意设计的，保护你的隐私）
- 清除浏览器数据，聊天记录就永久删除了，没有备份

---

## 功能介绍

### 私聊

点击「在线用户」页面里任意一个人，就可以开始聊天。

发送消息时，消息会在你的设备上加密，加密后才发出去。对方收到后在他的设备上解密，显示原文给他看。

**消息状态图标说明：**

| 图标 | 含义 |
|---|---|
| ○ | 发送中 |
| ✓ | 已发送（服务器已转发） |
| ✓✓ | 对方已读（对方打开了聊天页面） |
| ✗ | 发送失败（网络断开或对方不在线） |

**支持发送：**
- 文字消息（支持换行：Shift + Enter，最多 5000 字）
- 图片
- 任意格式的文件（最大 1GB）

### 文件传输

WebChat 发文件的方式和普通工具不一样，**文件不会先上传到服务器再下载**，而是直接从你的设备传到对方设备。

**发文件的步骤：**
1. 聊天页面点击 📎 图标，选择要发送的文件
2. 超过 100MB 会弹出一个提示，确认后开始传输
3. 等对方接受

**收文件的步骤：**
1. 对方发来文件后，屏幕顶部会出现一个绿色横幅：「某某 想发给你：文件名（大小）」
2. 点「接受」，浏览器会弹出一个"选择保存位置"的对话框
3. 选好位置，文件开始传输，屏幕上有进度条
4. 传完后会提示「已保存到你选择的位置」

**注意：**
- 传输过程中不要关闭这个页面，也不要切换网络
- 对方也必须保持在线，一旦对方离线传输会中断
- 文件也是加密传输的，每一片数据到达服务器时都是乱码

### 群聊

在「群组」页面可以创建群或者等待别人邀请你进群。

**创建群组：**
1. 点击右上角「+ 创建」
2. 填写群名，确定
3. 创建成功后，点击群旁边的「邀请」，把在线的用户加进来

**群组规则：**
- 只有群主才能邀请新成员
- 群主退出 = 解散整个群，所有人都会被移出
- 普通成员退出后，群密钥自动更换，他看不到你们之后发的消息

### 在线状态

「在线用户」页面实时显示当前在线的人，不需要手动刷新。

谁上线谁下线，你的列表会在几秒内自动更新。

### 消息通知

如果你切换到了其他标签页或者最小化了浏览器：
- 浏览器标签页的标题会显示未读消息数，比如「(3) WebChat」
- 如果你允许了通知权限，还会弹出系统通知

### 安装到手机桌面

WebChat 支持添加到手机主屏幕，打开后和普通 App 一样全屏显示，没有浏览器地址栏。

**iPhone：** Safari 打开 → 点底部分享按钮 → 「添加到主屏幕」

**Android：** Chrome 打开 → 点右上角菜单 → 「添加到主屏幕」或「安装应用」

---

## 使用建议

### 关于账号

- 用户名一旦注册无法修改，起名时想清楚
- 密码建议用一个你能记住但别人猜不到的，不要用生日或「123456」
- 同一个账号可以在手机和电脑同时登录，消息会同步推送到所有设备

### 关于聊天记录

- 聊天记录只存在当前设备的浏览器里
- 换手机、换浏览器，历史记录不会跟过去，这是正常的
- 如果你想保留某段对话，截图是最稳妥的方式
- 清除浏览器缓存/数据会一并删除聊天记录，清之前注意一下

### 关于文件传输

- 发大文件（比如几百MB的视频）时，双方都要保持页面打开
- 如果网络不稳定，传输可能中断，需要重新发
- 接收文件时，建议提前想好存在哪里，选好保存路径再点接受

### 关于群组

- 群名全局唯一，如果提示"名称已被使用"就换一个
- 群主不想解散群但想离开，目前暂不支持转让群主，只能让其他人新建一个群

---

## 常见问题

**Q：打开页面后很久才能登录，显示"服务器正在唤醒"是什么意思？**

这个服务部署在会自动休眠的平台上，一段时间没人用后服务器会进入睡眠状态。你是当天第一个访问的人，需要等服务器唤醒，通常 10～30 秒内完成。倒计时结束后会自动重试，不需要手动刷新页面。

**Q：换手机后打开 App，账号直接登录了，是怎么做到的？**

如果你的账号和密码保存在新设备上（比如浏览器自动填充），WebChat 会在 Token 失效时自动用保存的凭据重新登录，整个过程在后台完成，不会打断你的操作。

**Q：消息发出去了但显示"发送失败"怎么办？**

通常是网络断开导致的。检查网络连接，重新进入聊天页面后重发。

**Q：对方一直显示不在线怎么回事？**

对方可能真的没打开 WebChat，或者网络有问题。在线状态基于心跳检测，连续 3 次心跳无响应就会被判定为离线。

**Q：历史消息不见了？**

可能是清除了浏览器数据，或者换了浏览器/设备。聊天记录只存在本地，无法找回。

**Q：换了手机，原来的账号还能用吗？**

账号可以在新设备登录，新设备会自动生成一套新的加密密钥，可以正常收发新消息。但有两点需要注意：

- 旧设备上的历史记录不会迁移到新设备
- 新设备登录后旧密钥同时作废，旧设备上也会出现加解密问题

因为加密密钥和设备绑定，换机相当于"密钥换代"。建议换机前在旧设备上截图保存重要的聊天内容。

**Q：有人能偷看我的消息吗？**

技术上，服务器管理员、黑客、或者网络运营商，都无法读取你的消息内容，因为服务器上存的和网络上传的都是密文，没有对应的解密钥匙就是一堆乱码。能看到消息原文的只有：你自己，以及你发消息的那个人。

**Q：WebChat 会收费吗？**

不会。这只是多一种选择的小众社交方式。

---

## 隐私一览

| 服务器知道什么 | 服务器不知道什么 |
|---|---|
| 你注册了一个账号 | 你和谁聊了什么 |
| 你在线还是离线 | 你发了什么文件 |
| 你属于哪些群组 | 群里聊了什么 |
| 你的昵称和头像 | 你的聊天历史 |

一句话总结：**服务器只知道你在用这个工具，不知道你用它聊了什么。**
